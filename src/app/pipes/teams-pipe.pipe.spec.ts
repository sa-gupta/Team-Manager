import { TeamsPipePipe } from './teams-pipe.pipe';

describe('TeamsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TeamsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
